# OTA-AI-agents
To design a OTA AI agent that plans, schedules,rollout and rollback campaigns, this POC demonstrates  a multiclient grouped broadcasting system that sends large amount of different campaigns which is prioritized and scheduled using RR algorithm and sent to the respective client groups 
